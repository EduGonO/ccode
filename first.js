let touchX = -1;
let touchY = -1;
let ellipseColor;
let t = 0; // time variable

function setup() {
  let canvas2 = createCanvas(windowWidth, 420);
  canvas2.parent('code2');
  colorMode(HSB, 255);
  background(15, 30, 50);
}

function draw() {
  background(15, 30, 50);
}

